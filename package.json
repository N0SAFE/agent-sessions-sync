{
  "name": "agent-sessions-sync",
  "displayName": "Agent Sessions Sync",
  "description": "Keep your AI agent sessions (Claude Code, Codex, Cursor) synchronized across all of your computers via your own private GitHub repository.",
  "version": "0.2.0",
  "publisher": "Gregor-von-Vitek",
  "license": "MIT",
  "icon": "media/icon.png",
  "galleryBanner": {
    "color": "#0F766E",
    "theme": "dark"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Gregor-von-Vitek/agent-sessions-sync"
  },
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "claude",
    "claude code",
    "sessions",
    "history",
    "sync",
    "ai",
    "agent"
  ],
  "extensionKind": [
    "ui",
    "workspace"
  ],
  "capabilities": {
    "virtualWorkspaces": true,
    "untrustedWorkspaces": {
      "supported": true
    }
  },
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "agentSessionsSync.setup",
        "title": "Set Up / Change Repository",
        "category": "Agent Sessions Sync"
      },
      {
        "command": "agentSessionsSync.syncNow",
        "title": "Sync Now",
        "category": "Agent Sessions Sync"
      },
      {
        "command": "agentSessionsSync.resolveConflicts",
        "title": "Resolve Conflicts",
        "category": "Agent Sessions Sync"
      },
      {
        "command": "agentSessionsSync.mapClaudeProject",
        "title": "Map Claude Project Folder",
        "category": "Agent Sessions Sync"
      },
      {
        "command": "agentSessionsSync.openRepository",
        "title": "Open Repository on GitHub",
        "category": "Agent Sessions Sync"
      },
      {
        "command": "agentSessionsSync.showLog",
        "title": "Show Log",
        "category": "Agent Sessions Sync"
      },
      {
        "command": "agentSessionsSync.openMenu",
        "title": "Open Sync Menu",
        "category": "Agent Sessions Sync"
      }
    ],
    "configuration": {
      "title": "Agent Sessions Sync",
      "properties": {
        "agentSessionsSync.agents.claude.enabled": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Synchronize **Claude Code** sessions."
        },
        "agentSessionsSync.agents.claude.path": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "markdownDescription": "Local Claude Code sessions directory. Leave empty for the default `~/.claude/projects`. Supports `~`."
        },
        "agentSessionsSync.agents.claude.projectPaths": {
          "type": "object",
          "default": {},
          "scope": "machine",
          "additionalProperties": {
            "type": "string"
          },
          "markdownDescription": "Map Claude Code project folders in the repository to the project's directory on **this** machine, so sessions synced from machines with different project paths show up in `claude --resume`. Keys are folder names under `claude/` in the repository (another machine's encoded project path), values are local project directories (supports `~`). Prefer the **Agent Sessions Sync: Map Claude Project Folder** command — it also moves already-downloaded sessions. A mapping stays inactive while a local sessions folder with the repository name still exists."
        },
        "agentSessionsSync.agents.codex.enabled": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Synchronize **Codex** sessions."
        },
        "agentSessionsSync.agents.codex.path": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "markdownDescription": "Local Codex sessions directory. Leave empty for the default `~/.codex/sessions`. Supports `~`."
        },
        "agentSessionsSync.agents.cursor.enabled": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Synchronize **Cursor** sessions."
        },
        "agentSessionsSync.agents.cursor.path": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "markdownDescription": "Local Cursor sessions directory. Leave empty for the default `~/.cursor/chats`. Supports `~`."
        },
        "agentSessionsSync.agents.vscode.enabled": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Synchronize **VS Code Copilot Chat** sessions."
        },
        "agentSessionsSync.agents.vscode.path": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "markdownDescription": "Local VS Code workspace storage directory. Leave empty to use the currently-running VS Code instance's workspace storage automatically. Supports `~`."
        },
        "agentSessionsSync.agents.vscode.workspacePaths": {
          "type": "object",
          "default": {},
          "scope": "machine",
          "additionalProperties": {
            "type": "string"
          },
          "markdownDescription": "Map VS Code workspace folders in the repository to their directory on **this** machine, so sessions synced from machines with different project paths are restored into the right workspace. Keys are folder names under `vscode/` in the repository (another machine's encoded project path), values are local project directories (supports `~`). Without a mapping, a workspace only restores when the same absolute path exists on this machine."
        },
        "agentSessionsSync.autoSync": {
          "type": "boolean",
          "default": true,
          "description": "Automatically synchronize sessions on startup, on file changes and periodically. When disabled, use the 'Sync Now' command."
        },
        "agentSessionsSync.pollIntervalMinutes": {
          "type": "number",
          "default": 5,
          "minimum": 1,
          "maximum": 720,
          "description": "How often (in minutes) to check the remote repository for changes made on other devices."
        },
        "agentSessionsSync.debounceSeconds": {
          "type": "number",
          "default": 30,
          "minimum": 1,
          "maximum": 600,
          "description": "How long to wait after a local session file change before syncing (debounce). Sessions are written continuously while an agent runs, so keep this generous."
        },
        "agentSessionsSync.freshMinutes": {
          "type": "number",
          "default": 5,
          "minimum": 0,
          "maximum": 120,
          "description": "Session files modified within this many minutes are considered actively in use and are skipped in both directions until they settle. 0 disables the check."
        },
        "agentSessionsSync.maxFileSizeMB": {
          "type": "number",
          "default": 50,
          "minimum": 1,
          "maximum": 95,
          "description": "Session files larger than this are not synchronized (GitHub blobs are limited to 100 MB)."
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "pretest:integration": "npm run build && tsc -p tsconfig.integration.json",
    "test:integration": "node out-test/test/integration/runTest.js",
    "vscode:prepublish": "node esbuild.mjs --production",
    "package": "vsce package",
    "install:local": "node scripts/install-local.mjs",
    "install": "npm run install:local"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/sql.js": "^1.4.11",
    "@types/vscode": "^1.90.0",
    "@vscode/test-electron": "^2.4.1",
    "@vscode/vsce": "^3.1.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "sql.js": "^1.14.2"
  }
}
